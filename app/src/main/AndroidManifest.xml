<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pawansiwakoti.vicroadslicensetest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/vicroads"
        android:label="@string/app_name"
        android:roundIcon="@drawable/vicroads"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Video"></activity>
        <activity android:name=".activities.Email" />
        <activity android:name=".activities.HomePage" />
        <activity android:name=".activities.PrepareFortest" />
        <activity android:name=".activities.Result" />
        <activity android:name=".activities.QuizActivity" />
        <activity android:name=".activities.GeneralActivity" />
        <activity android:name=".activities.Register" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>